import { Inter as FontSans, Sora as FontSerif } from 'next/font/google'

export const fontSerif = FontSerif({ subsets: ['latin'], variable: '--font-serif' })
export const fontSans = FontSans({ subsets: ['latin'], variable: '--font-sans' })
